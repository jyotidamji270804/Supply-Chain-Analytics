# Supply Chain Analytics — Demand Forecasting & Anomaly Detection

Infotact Technical Internship Program — Project 3.

An end-to-end time-series analytics pipeline that forecasts product demand
and flags supply-chain anomalies, delivered as an interactive Streamlit dashboard.

## Setup

bash
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt

More details coming as the project builds out over the next 4 weeks.